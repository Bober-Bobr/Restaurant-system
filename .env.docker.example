# Copy to ".env" next to docker-compose.yml and set a strong password.
# docker compose reads this file automatically.
POSTGRES_USER=vmenu
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=vmenu
